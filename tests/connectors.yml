# general metadata
version: "CHANGEME"
repository: "git@github.com:elastic/connectors-ruby.git"
revision: "main"

elasticsearch:
  hosts: http://localhost:9200
  api_key: "3YTZkm4VQYSSkSlMQlSmlg"

connector_id: "mongo"
service_type: "mongo"
log_level: info

poll_interval: 60
